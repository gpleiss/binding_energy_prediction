# GPR Prediction of Binding Energies of Astrochemically Relevant Mollecules

Requires Python >= 3.10

## Required packages
Run `pip install -r requirements.txt`.

## Scripts
See `notebook` folder for Jupyter notebooks.
